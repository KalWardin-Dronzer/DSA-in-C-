/*switch (expression) {
case x:
// code
break;
case y:
// code
break;
default :
// code
}
*/

// take input form the user and print whether it is vowel or consonant.
#include <iostream>
using namespace std;
/*int main() {
    char alphabet;
    cout<<"Enter an alphabet: ";
    cin>>alphabet;
    if (alphabet == 'a' || 'e' || 'i' || 'o' || 'u') {
        cout<<"it is a vowel"<<endl;
    }
    else {
        cout<<"it is a consonant"<<endl;
    }

}*/
// this code is wrong, why?


/*
switch (alphabet){
    case 'a':
    cout<<"lt is a vowel"<<endl;
    break;
    similarly other cases,
    default:
        cout<<"consonant"    */